require 'spec_helper'

describe Realdebrid do
  it 'has a version number' do
    expect(Realdebrid::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
